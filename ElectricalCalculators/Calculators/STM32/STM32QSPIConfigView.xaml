<UserControl x:Class="ElectricalCalculators.Calculators.STM32.STM32QSPIConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ElectricalCalculators.Calculators.STM32"
             xmlns:conv="clr-namespace:ElectricalCalculators.Utils.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <UserControl.Resources>
    <conv:SuffixConverter x:Key="SuffixConverter"/>
    <conv:ByteConverter x:Key="ByteConverter"/>
  </UserControl.Resources>
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <Border Grid.Column="0">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0">Flash Size</TextBlock>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path=FlashSizeBits, Converter={StaticResource ByteConverter}}"/>
        <TextBlock Grid.Row="0" Grid.Column="2">Bits</TextBlock>

        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Path=FlashSizeBytes, Converter={StaticResource ByteConverter}}"/>
        <TextBlock Grid.Row="1" Grid.Column="2">Bytes</TextBlock>

        <Separator Grid.Row="2" Grid.ColumnSpan="3"/>

        <TextBlock Grid.Row="3" Grid.Column="0">MX Flash Size</TextBlock>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Path=SizeConfigRaw, StringFormat=F3}" IsReadOnly="True"/>
        <TextBlock Grid.Row="3" Grid.Column="2">Raw</TextBlock>
        
        <TextBlock Grid.Row="4" Grid.Column="0">MX Flash Size</TextBlock>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Path=SizeConfigOutput}" IsReadOnly="True"/>
        <TextBlock Grid.Row="4" Grid.Column="2">Output</TextBlock>

        <Separator Grid.Row="5" Grid.ColumnSpan="3"/>

        <Button Grid.Row="6" Grid.ColumnSpan="3" Command="{Binding Path=CalcCmd}">Calc</Button>
        <Button Grid.Row="7" Grid.ColumnSpan="3" Command="{Binding Path=ClearCmd}">Clear</Button>
      </Grid>
    </Border>

    <Image Grid.Column="1" Source="/Resources/STM32_QSPI_CalcSize.webp"/>
  </Grid>
</UserControl>
