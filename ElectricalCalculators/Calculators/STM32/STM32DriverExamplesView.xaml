<UserControl x:Class="ElectricalCalculators.Calculators.STM32.STM32DriverExamplesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ElectricalCalculators.Calculators.STM32"
             d:DataContext="{d:DesignInstance Type=local:STM32DriverExamplesViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <Grid Grid.Column="0">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <Button Grid.Row="0" Command="{Binding UpdateRootFolderCmd}">Select Root Folder</Button>
      <TextBlock Grid.Row="1" Text="{Binding RootFolderName}"/>
      <Separator Grid.Row="2"/>
      <TextBlock Grid.Row="3">Search Pattern</TextBlock>
      <TextBox Grid.Row="4" Text="{Binding Settings.STM32ExampleFolderSearchPattern}"/>
      <Separator Grid.Row="5"/>
      <TextBlock Grid.Row="6">Folder Open Extension</TextBlock>
      <TextBox Grid.Row="7" Text="{Binding Settings.STM32ExampleOpenFolderExtension}"/>
      <CheckBox Grid.Row="8" IsChecked="{Binding Settings.STM32ExampleOpenWithFolderExtension}">Use Extension</CheckBox>

      <Button Grid.Row="10" Command="{Binding OpenRootFolderCmd}">Open Root Folder</Button>
    </Grid>

    <Grid Grid.Column="1">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <TextBlock>Examples</TextBlock>
      <DataGrid Grid.Row="1" ItemsSource="{Binding ExampleFolders}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False">
        <DataGrid.Columns>
          <DataGridTextColumn Header="Series" Binding="{Binding Name}"/>
          <DataGridTextColumn Header="Version" Binding="{Binding Version}"/>
          <DataGridTemplateColumn Header="">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <Button Click="OpenFolder_Click">Open</Button>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
        </DataGrid.Columns>
      </DataGrid>
      <!--<ListView Grid.Row="1" ItemsSource="{Binding ExampleFolders}">
        <ListView.ItemTemplate>
          <DataTemplate>
            <Border>
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding Name}"/>
                <Button Grid.Column="1" Click="OpenFolder_Click">Open</Button>
              </Grid>
            </Border>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>-->
    </Grid>
  </Grid>
</UserControl>
